name: flutter_target_test
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter 
  mobx: ^2.2.3 
  flutter_mobx: ^2.2.0+1
  url_launcher: ^6.2.2
  shared_preferences: ^2.2.2
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^2.0.0 
  build_runner: ^2.4.7
  mobx_codegen: ^2.4.0

flutter:
  uses-material-design: true
